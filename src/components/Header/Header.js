import React from 'react'
import styled from 'styled-components'

const Header = () => <S.Header>Header</S.Header>

const S = {
  Header: styled.header`
    display: flex;
    width: 100%;
  `
}

export default Header
